<% private_conversations_windows.each do |conversation| %>
  <%= render partial: @private_conversations_windows,
             locals: { conversation: conversation, 
                       user: current_user } %>
<% end %>